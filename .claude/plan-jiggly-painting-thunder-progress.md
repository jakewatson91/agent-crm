# Progress log: jiggly-painting-thunder plan (2026-07-17)

Plan: `~/.claude/plans/jiggly-painting-thunder.md` — research → scoring → drafting throughput day.
Resume rule: work top to bottom. Each item flips to DONE with commit hash + what was verified. If a session dies mid-item, the "state" line says exactly where to pick up.

## Status

- [x] #0 DONE (code side): research.ts angle fallback num_results 4→3; research_strategy.ts DEFAULT_NUM_RESULTS 4→3; domains.ts resolver 5 results/300 chars → 3 results/600 chars, doc comments updated. NOT committed yet (single commit at end). Jake side still open: $10 Exa top-up at dashboard.exa.ai, confirm Inngest billing card attached.
- [x] #1 DONE (code + config + verified, not committed): domainBackfillDaily cron in system_tasks.ts (registered in index.ts + route.ts), policy.research.domain_backfill_per_day knob (packages/tools/src/policy.ts), Sudden knob set to 75 via _cfg_domain_backfill_knob.ts. Verified: scan returns 0 while Sudden is research-paused / others opted out; single-entry batch against live Exa 402 → search_error → pause path rewrote the credit-wall status correctly (script _run_backfill_verify.ts). Backlog measured: 1,961 Sudden accounts, 215 with domain, 1,746 without (_chk_domainless_count.ts). Live backfill starts when Jake tops up Exa + clicks Continue. NOTE: entities has NO type column — account = is_a fact via entityIdsOfType.
- [x] #2 DONE (code + verified, not committed): pool C in rescoreOnIcpChange — 72h fact-lookback window (RESCORE_FACT_LOOKBACK_MS; full-table scan would repeat the egress mistake), compares latest substantive fact vs current icp_fit observed_at, unified noop-marker rule `marker > max(cfgMs, latestFactMs)`. Refactored scan/batch into exported scanRescoreCandidates/runRescoreBatch (same pattern as #1) so scripts run shipping code. Verified live: scan found 15 (14 Sudden = exact measured backlog + 1 demo), batch rescored 15/15, second scan = 0 (no churn), scores refreshed (0.78→0.77, 0.77→0.75, rest confirmed same). Scripts: _run_rescore_verify.ts, _chk_poolc_backlog.ts, _chk_rescored_values.ts. NOTE: facts values live in object_text (no object_num column).
- [x] #3 DONE (code + config + verified offline, not committed): policy.ts domain_scope union + social_domains knob; research_strategy.ts VALID_SCOPES + socialScopeAddendum appended to SYS_PROMPT only when social_domains configured (planner never plans inert angles); PlannerContext.social_domains via loadContext; research.ts social branch in buildAngleRequest (now exported for scripts) + runnable filter skips social when unconfigured (own_site slot-burn bug pattern); settings Angle type widened. Sudden config applied via _cfg_sudden_social.ts: guidance (exec posts/talks/interviews bias) + social_domains=['linkedin.com'] + strategy regenerated (planner produced social_posts angle, recency 60, n=3) + persisted. Verified _chk_social_angle_build.ts: include_domains=['linkedin.com'] + start_published_date + include_text; null when domains empty. Gate: social ≠ own_site so disambiguation applies automatically. Live search verify still pending Exa top-up. NOTE: research policy writes do NOT bump scoring_config_state (hash covers icp/about/persona/scoring only) — no accidental rescore.
- [x] #4 DONE (code + config + verified, not committed): Sudden enrichment set via _cfg_sudden_contacts.ts — contact_provider explorium (was hunter), contact_provider_fallback hunter, max_contact_pulls_per_run 16 (was 3, NOT the assumed 8); both API keys confirmed present. action_selector.ts watch_only branch now composes reason from actual failed draft conditions (icp below draft bar / signal / evidence / no scored contact / contact below bar / suppression window); policy string 'fit_weak_trigger' unchanged. Verified via _chk_watch_reasons.ts: all four crafted cases name the true blocker.
- [x] #5 DONE (code + verified, not committed): draftAuditFlags exported pure fn in agent_logic.ts + non-blocking call after the reasoning post (kind:'system', parent=draft post, try/catch so a check failure never blocks a draft). Checks: char budget +10% grace (mirrors prompt's effective budget: explicit ?? 400 on the linkedin+templates path), URL-in-body, reasoning names a template (id/label/[n]/"template n"; same usability filter as buildDrafterDecision). Verified _chk_draft_flags.ts vs Sudden's real 4-template config: bad draft → all 3 flags, good draft → 0. End-to-end system post lands on next natural draft.
- [x] Final DONE: typecheck clean on tools/inngest/apps-web (pre-existing failures only: primitives .ts-extension errors, primitives/llm.ts, tools/diff_draft.ts, web signals route cast). Committed 9f23946, pushed to main → Render deploy.

## State

Session 1 (2026-07-17): #0 code side done.
Session 2 (2026-07-18): #1–#5 all done, verified, committed 9f23946, pushed. PLAN COMPLETE except Jake-side items below.

## Notes / decisions

- Exa topped up 2026-07-18 (Jake, mid-session). Research pause cleared via _continue_sudden.ts (same write as the Continue button). LIVE POST-TOPUP VERIFY: backfill scan returned 75 (knob cap), 8-entity batch resolved 6 (TG4→tg4.ie, Pilot WP→pilot.wp.pl, RTVC Play→rtvcplay.co, Disney NOW, Kocowa, CMGO), 2 clean no-matches ("Chili" refused, guard working). Full research run on DramaBox (0.72, dramabox.com): 5 searches, social_posts angle → 3 real LinkedIn exec-post signals (Head of Studio posts, LatAm-expansion interview, VidCon), 6 collisions dropped by the gate, 7 signals total. Local "enricher dispatch 401" is expected (no Inngest event key on laptop); recover-unmatched-signals re-emits in cloud within ~45 min.
- Inngest billing RESOLVED 2026-07-18: IN-XS is the FREE tier in Inngest's new SKU model (500K events / 100K depth / 5 concurrency; paid starts at IN-S $99). Executions are NOT billed or capped on it — the plan's "158K vs 50K included, ~$5.40 overage" claim was wrong (mapped old Hobby/Pro public pricing onto the account). No card needed. All enforced dims green at 14% events, cycle 53% done.
- Demo workspace manual pause (scope=all) left untouched, intentional.
- Config applied to Sudden (live, not code): research.domain_backfill_per_day=75; research.social_domains=['linkedin.com']; research.guidance=exec-posts bias; strategy regenerated (5 angles incl. social_posts recency=60 n=3); enrichment.contact_provider=explorium, fallback=hunter, max_contact_pulls_per_run=16 (was hunter/none/3).
- Pre-existing typecheck failures are NOT ours: packages/primitives (TS5097 .ts imports + llm.ts undefined), tools/diff_draft.ts, web sources/[source_id]/signals route cast.
- entities table has NO type column; account membership = is_a facts (entityIdsOfType). Facts values live in object_text.
- policy.research writes do NOT bump scoring_config_state (hash = icp/about/persona/scoring/contact_scoring/personas/scorable_types only).
