# Project State

Last Update: 2026-08-04

> Open items + reference only. Dated session history → `progress_log.md`. Current system map → `architecture.md`. Cross-project lessons → agent_memory daily log.

## Next session — read first
**2026-08-04 (last) — `research.max_age_days` 30 → 90 on Sudden, deliberately matched to the drafter's `trigger_max_age_days` (90).** Research was discarding at ingestion the exact material the drafter would have used: it will build a theme-led message on a 60-day-old fact, it just will not lead with it as news (`trigger_fresh_days` 14, unchanged). Measured on 8 accounts before the change: 29 results fetched, paid for, then binned as stale. After, on a re-run of the top 4: Cineverse 0 → 3 signals, Ab Films stale drops 5 → 1, Weyyak 2 → 1. **Costs no extra searches** (these are results already fetched) but more surviving signals means more enrichment calls, which is where the model spend is. Revert by deleting the key if enrichment volume looks wrong.
- **ShowMax is not a pipeline failure — stop chasing it.** It dropped 10 results as stale both before AND after the threshold moved 30 → 90, which means all 10 are over 90 days old, plus 7 filtered as off-topic. It has no recent web material matching Sudden's angles. The drafter gating it is correct. Giving it a domain was still right (it was invisible to research entirely), it just was not the blocker.
- **Local runs cannot dispatch.** `_trigger_research_sudden.ts` reports `dispatched: 0, dispatch_errors: N` and the runner reports `enricher dispatch failed: 401 Event key not found` — both are a missing local Inngest event key, not a bug. Use `scripts/_run_research_now.ts` (calls the runner core directly). Signals created that way are not enriched locally; production's 15-min `recoverUnmatchedSignals` cron picks them up. Confirm with `pnpm research:check`.

**2026-08-04 (latest) — RESOLVED for the top of the book. Run `scripts/_run_domain_backfill_now.ts --apply` AFTER ANY FULL-BOOK RESCORE.** The domain backfill orders candidates by `icp_fit` (shipped 07-28, `9e33a4a`), so a rescore that reranks the book only changes who gets the budget on the NEXT backfill run. The sequence that bit us: backfill ran 07-29 to 08-01 on the old scores, this morning's rescore made ShowMax 1.00 and Cineverse 0.95, and the cron has not fired since 08-01 — so the two best accounts in the book stayed domainless and unresearchable. Ran it manually: **ShowMax → showmax.com, Cineverse → cineverse.com**, 43 of 75 resolved, 32 no_match, 0 errors, ~$0.53. No code change was needed; the design was already right.

**"Half the top accounts have nothing to write on" is a COVERAGE problem, not a research-quality one. Two things to fix, neither is a prompt.** (1) **1127 of 1961 accounts have no `attributes.domain`, and a domainless account is never a research candidate by design** (`entity_research_dispatcher.ts:365`). ShowMax — the highest-scored account in the book at 1.00 — and Cineverse (0.95) have `researched=never` for this reason, which is exactly why they gate for "no recent dated event". `domain-backfill-daily` fixes this, is configured at 75/day (~15 days, ~$11 of Exa), and **last ran 2026-08-01 despite a `0 11 * * *` cron**. (2) Among the 834 researchable accounts, research touched 49 distinct accounts in 6.8 days = **one visit per ~116 days**, against a 14-day freshness bar for a trigger-led draft.
- **Research itself is healthy — do not "fix" the angles or the freshness gate.** Since the 07-29 freshness fix, 53% of new research signals come from pages ≤30 days old (9% across the whole corpus). Top accounts that HAVE been researched hold pages 1-8 days old (Globoplay 1d, M6+ 6d, FloSports 8d). Extraction shape is fine too: the freshest signals yield `news_event`, `recent_event`, `pain_observed`, `streaming_scale`. The corpus only LOOKS profile-heavy because 2536 of 4226 signals are the CSV import, 0% dated by nature.
- **WATCH: zero `research_triggered` on 08-02 and 08-03, 18 on 08-04; `domain_resolved` silent since 08-01.** `pipeline_status` reads ok with a 14:39 run today. Same shape as the recurring host/keepalive failure — confirm the cron-job.org keepalive before assuming it is healthy.
- Free lever, no spend: `research.selection_mix` is unset (default 55/30/15). Shifting toward `high_value` concentrates the existing budget on accounts that would actually be drafted. Paid lever: `searches_per_run` is unset (default 30/tick); each +10 is ~+60 searches/day, ~$0.60/day.
- **Cost reference for this class of work, measured from `DEFAULT_PRICING`:** both the enricher-style backfills and the scoring rubric run on `deepseek-v4-flash` ($0.14/1M in, $0.28/1M out). A full-book pass over ~1900 accounts is ~$0.75, and the rescore it triggers is ~$1.70. Full-book LLM passes here are dollars, not tens of dollars. Exa is the expensive input, not the models.

**2026-08-04 (later) — the drafter now picks its argument BEFORE it sees an exemplar. UNCOMMITTED; the automated drafter keeps the old behaviour until this is committed and Render redeploys.** New `policy.drafter.templates[].angle` (one sentence naming what that exemplar argues) + `packages/tools/src/pick_angle.ts`, one `deepseek-v4-flash` call per drafted account that picks a problem from `pain_points` and names which templates argue the same thing. Those render `EXEMPLAR: WITHHELD` plus their anatomy; the menu collapses to the one chosen problem. Any failure falls back to the exact old prompt, so the picker can never block a draft. Sudden's four templates are tagged already (`scripts/_set_sudden_template_angles.ts`, applied). **The sameness moved rather than disappeared:** 5 of 7 accounts still get `pain_points[0]`, which is written as the answer to the question every draft asks, so the drafts still rhyme even with nothing to copy. That is now a config question — Sudden's four pain points are one problem at four zoom levels — and it is Jake's call. Full measurement + the two options: `.claude/drafter_quality_plan.md`, "Item 5's remaining half".
- **The `buildSystemPrompt` → `buildDrafterDecision` hand-off has now silently eaten a THIRD field** (`angle`, after `templates` in `f101935` and `char_budget` on 07-21). It re-listed ~17 optional keys by hand, so a missing one type-checks clean. It spreads the object now; do not go back to re-listing keys. `pnpm tsx scripts/_chk_drafter_prompt.ts` is what caught it and now asserts the withheld-exemplar path too — run it after any drafter prompt change.
- **The pitch menu was rewritten around one product fact Jake confirmed: offload needs viewers watching the same title AT THE SAME TIME, and the target is back catalogue, not live.** `scripts/_set_sudden_pitch_menu.ts`, applied. `pain_points` 4 → 3 (the 1:1 entry is gone from the menu; it is the constitution's cred claim, not a problem, and leaving it there handed the model the answer to ask back). `value_props` 4 → 5 (entry 1 reworded, three objection handlers untouched, and a new "runs without anyone on the video team owning it day to day" because the old menu had a staffing pain with nothing answering it). Neither field is in `scoreInputsHash`, so no rescore. **Do NOT re-add anything about premieres, live matches or launch peaks** — `out_of_scope` condition 1 vetoes live-only accounts, so that menu entry would aim the drafter at the accounts the veto exists to exclude. Binge viewing and long-tail rewatch are out too: viewing spread over time forms no swarm.
- **The concurrency floor is now a research question, not a veto.** `research.always_include` carries "peak concurrent viewers or simultaneous streams on a single title" and the planner authored `open_peak_viewers` (open_web, 365d) for it. Revisit the floor once there is coverage. Still do NOT make it an `out_of_scope` condition: a binary test cannot fire on a book that holds no concurrency facts.
- **GOTCHA before editing any research config: `isStrategyFresh()` checks only the AGE of `strategy_generated_at`.** Nothing watches `guidance` or `always_include`, so editing either does nothing until the cached strategy ages out. Force a regeneration (`generateResearchStrategy` + `persistResearchStrategy`, as `scripts/_set_sudden_stable_attrs_and_research.ts` does) or the edit is inert. Real fix would be hashing the planner inputs the way `scoreInputsHash` does for scoring.
- **The out-of-scope veto now reads stored answers instead of re-deriving them from contradictory prose.** OVI Technologies scored 0.95 while its own site says "when we say Live, we mean LIVE" — because an imported `product: Film/TV Streaming` tag contradicted its description, and the rubric correctly refused to veto on conflicting evidence. New `policy.enrichment.stable_attributes` (`{predicate, question, values[]}`, empty by default, contents are customer config) + `scripts/backfill_stable_attributes.ts` (dry run default; drops any value outside the configured list; "unknown" stores nothing). Sudden runs `delivery_mode` and `business_model` — the latter is exactly the predicate drafter-plan item 2 asked for. **OVI verified: icp_total 0.95 → 0, written.** NOT run across the book: sample of 25 answered delivery_mode for 7 (3 live_only) and business_model for 20, so a full pass may veto ~200 accounts, and costs ~1800 cheap-model calls plus a rescore of every touched account. Jake's call.
- **`pain_points` and `value_props` are re-derived from the About text by `deriveDefaults`,** so saving About in Settings silently overwrites both lists. If the new menu holds up, fold it back into About.
- **WATCH after deploy: `angle_outcome` on the `drafter_shortlist_pick` event.** Values are `picked` / `no_problem_fits` / `llm_error` / `unparseable` / `no_facts` / `menu_too_small`. Mostly `no_problem_fits` means the picker is refusing rather than choosing and every draft has quietly reverted to the old full-menu prompt.

**2026-08-04 — out-of-scope veto LIVE on main (`709f8f6`, pushed, Render deploys it). Empty by default; only Sudden has conditions.** `policy.drafter.out_of_scope` is a list of plain sentences describing accounts that cannot be served. The scorer forces `icp_total` to 0 on a match; the drafter refuses at STEP 0 with `account_out_of_scope`. It is a VETO, not a weighted dimension, and must stay one — `scripts/check_out_of_scope.ts` pins why (TVU's real sub-scores land at 0.78, or 0.71 with a whole dimension zeroed, against a 0.40 draft bar; averaging can never disqualify). Sudden's two conditions: live-only, and sells-infrastructure-to-streamers. Rescore of 1961 accounts vetoed 18 (0.8%), no false positives — AWS, Akamai, Edgio, CDN77, Kaltura, JW Player, Netskrt (a competitor). **WATCH:** a third condition was drafted and deleted before the rescore ("reaches viewers only through native/TV apps") because the binary test can never fire — do not re-add it in that shape; the real question is what SHARE of viewing is on web, which research should size. **COST:** `out_of_scope` is in `scoreInputsHash`, so editing a condition's wording re-rubrics the whole book. Get the wording right on paper first.
- **`changed_at` is no longer the only way a config change reaches the scorer.** `scoring.ts` now also compares the live `inputs_hash` against the one stored on `icp_fit_breakdown`, so a settings save or a script that forgets to bump `scoring_config_state.changed_at` can't silently no-op any more (the first rescore this session no-opped on ~3/4 of the book for exactly that reason). Costs nothing — a matching hash falls through to the reuse path with `llm_called=false`. Any NEW caller of `scoreInputsHash` must pass `outOfScope` or its hash disagrees with the scorer's (`recombine_scores.ts` was fixed for this; check any other call site added later).
- **Age now retires EVENTS, not standing state.** A dated fact describing what a company runs on survives as theme evidence past `trigger_max_age_days`; only events expire. This was found the hard way: dating Intigral's VisualOn case study pushed it past 90 days, the old rule called it dead weight, and the account went from drafting to gating *because a date was added*. Don't reintroduce a blanket age cut.
- **A theme may rest entirely on undated facts.** 75% of Sudden's signals carry no date at all and 61% have no URL, so theme-led mode is the only route most of the book has to a draft. `scripts/backfill_url_published_dates.ts` recovered only 7 of 4208 — URL parsing is not the lever.
- **OPEN, and the biggest remaining lever: split angle from template.** Templates are indexed by audience but each still implies one argument, so picking an audience picks a question. Rewriting the exemplars into an unrelated vertical (`scripts/_set_sudden_exemplars.ts`, freight) stopped verbatim copying and is most of the win, but the structural fix is to never render an exemplar whose angle matches the one being written. `t4_discovery` stays disabled; it was rewritten too so re-enabling it can't reintroduce a copyable video question.
- **OPEN: contact provider is wrong for the channel.** `enrichment.contact_provider` is `hunter` (an EMAIL finder) at 50/month against 1961 accounts, on a workspace whose `outreach_channel` is linkedin and never uses an email. `scripts/extract_contacts_from_notes.ts` bought 48 accounts back for free by promoting people already named in `prospect_notes` (50 contacts created, 119 accounts now have one), but that well is finite. Either switch to a provider returning role + profile, or drop the email requirement.
- Full findings, measured before/after drafts, and the remaining ranked work: `.claude/drafter_quality_plan.md`.

**2026-07-24 — Today page is now the full 24h recap and the intended landing page.** `apps/web/app/_lib/today.ts` + `TodayClient.tsx` + `TodayCharts.tsx`. Sections: counters, alerts, run log, movers, pages read (with per-angle yield), facts learned, contacts, drafts/declines/decisions, connectors, sources, spend, 14-day trend. Warm 0.17-0.34s across 4 workspaces, SSR 245-328KB. Two things not to undo: (1) `sweepWorkspace` is deliberately NOT called here (measured 8.3s) — the page's health checks are cheap targeted reads off data it already has; (2) the trend charts intentionally show no "today" number, because their buckets are UTC calendar days while every counter above them is a rolling 24h. Run rows are reconstructed by clustering job events on a 30-min gap, since there is no runs table. Chart marks use `--chart-ink`, not `--accent-blue`: the accent measures 2.42:1 on the light panel, under the 3:1 floor. New `scripts/_mint_session.ts` mints an authed cookie for curl/headless testing (cookie value is base64**url**; standard base64 400s).

See `.claude/backlog.md`. North star = more agent autonomy + phone-actionable approvals (Jake wants the system running itself and pinging him to accept/reject from his phone, including dial-now/snooze for call moments).

**2026-07-31 — WATCH `signals.structured_tags.published_at_unreadable` (`79f27ab`, pushed). Any row there is a date format the enricher prompt still fails to convert.** The enricher prompt asked for `source_published_date` "in YYYY-MM-DD form" but never said to convert what it found, and `parseContentDate` takes ISO only, so a page printing `23/04/2026` in its header came back verbatim and was discarded as silently as a page carrying no date at all. The prompt now owns the conversion including day/month order (page's own language decides; returns "" when nothing settles it), and `unreadableContentDate` parks a rejected-but-year-bearing value on the signal instead of dropping it. Query: `select count(*), structured_tags->>'published_at_unreadable' from signals where structured_tags ? 'published_at_unreadable' group by 2 order by 1 desc;` — empty means the prompt fix took; rows name the format to add. **Forward-only:** the raw model output was never kept, so signals already holding a wrong provider date have nothing to recover from. NOT BUILT (offered, Jake didn't take it): a one-off pass flagging existing signals where `published_at_source` is the provider and the body holds a date-shaped string that disagrees.

**2026-07-29 — Research freshness gate fixed (`209666c`, pushed); WATCH that Sudden's ticks going forward stay ≤30 days old.** `own_site` scope was fully exempt from the freshness gate (design assumption, not a bug at the time) — confirmed live: a 2021 launch announcement became a fresh signal same-day. Fixed in `inngest/functions/research.ts` + `research_strategy.ts`: gate now applies to every scope (only genuinely undated pages stay exempt), default max source age 365→30 days, own_site queries now send Exa a recency filter. Also tightened the relevance-check prompt to reject a page whose real subject is a DIFFERENT company (a vendor case study naming the target only as a customer/channel) — this is an LLM prompt change, unverified in production; no evidence yet it actually catches that pattern going forward. Sudden's live `policy.research.strategy` patched directly so the fix is in effect now. Purged 243 stale trigger/pain facts from Sudden (pain_observed/launches/hires/deals sourced from a >30-day-old article); deliberately LEFT IN PLACE (Jake's call): 968 static profile facts (founder/HQ/employee-count/description) sourced from old articles but still accurate, and all pipeline-bookkeeping facts (`outreach_stage`, `icp_fit_breakdown`). Don't re-flag those 968 as stale/wrong.

**RESOLVED 2026-07-28 — the 2,543 wrongly-archived af602fa1 accounts have been restored (`da02914`) and are staying restored.** The daily archive sweep had the same 1000-row-cap bug as the entities-search fix below — its "does this candidate have any facts/signals" check silently came back empty past 1000 ids, so every candidate looked activity-free and the sweep archived up to 500 real, fully-enriched accounts a day (Paramount+, Reuters, NHL Network, and hundreds more). Fixed in code (`00ae273`, chunk `.in()` at 150 + page with `fetchAll`); the restore itself ran 2026-07-28 and was confirmed via `pnpm tsx scripts/_chk_wrongarchive.ts`. No longer urgent, no follow-up needed.
- Same session also shipped duplicate-account detection: a merge proposal card on the entity page (generous matching — shared domain / similar name / distinctive brand token — filtered by a human) backed by an atomic `merge_accounts` RPC (migration 0046) that reassigns facts/signals/posts/contacts and archives the loser with `_merged_into`, reversible from the event log. Built to solve the NHL / NHL.TV / Rogers NHL Live cluster.
- UI pass same day (evening commits): dark mode (theme-tokenized, follows OS, manual toggle), one score language (bare numbers → "strong fit"/"weak fit" verdict pills), a "Today" home page (daily briefing replaces the raw-feed redirect), a relationship graph on the entity page (was a text list), and provenance shown as a vertical trace instead of a gray JSON dump. None of this is blocking — the archive-restore item above is.
- Daily digest email is live (15:15 UTC, opt-in `policy.report.daily_email`, currently only Sudden). Drafter "weld" prompt (beats → one voice) verified clean on a 6-account dry run. LinkedIn edit-then-approve no longer silently drops the human's edits.

**2026-07-21 — Drafter craft rules live (`50e8f80`, `dd8961a` pushed). WATCH the draft-decline rate: declining is now the correct majority outcome.** The LinkedIn template prompt now walks 9 steps (real trigger or stop → one-hop problem → answer-from-memory question → never ask for calendar time → line edit → re-check). Dry-run over 6 real Sudden accounts: 2 drafts, 4 gates. That ratio is the design working, not a breakage — do NOT "fix" a drop in draft volume by loosening the trigger rule. If volume matters, the lever is more fresh facts (research/ATS yield), not a laxer drafter. Grade any prompt change with `pnpm tsx scripts/_dryrun_drafts.ts [n]` before it can reach a send approval — it builds the real prompts, writes nothing, and greps for banned CTAs / unsourced claims / filler.
- New knob `policy.drafter.trigger_max_age_days` (default 90, unset everywhere). It is the ONLY part of the craft block that varies by customer — how fast a trigger goes stale is a property of their market. Everything else is craft and stays in code.
- `draftAuditFlags` now also flags a draft that asks for a meeting or contains no question. Shapes only; customer phrase bans stay in `policy.outreach.banned_phrases`.
- WATCH parse failures: one of ~10 dry-run completions came back as unparseable JSON, cut mid-body. Live drafter and the dry-run share `max_tokens: 1500` (`agent_logic.ts:690`), and the craft block asks for a longer `reasoning` field than before. Did not reproduce on a 6-account rerun, so it is unconfirmed — if real drafts start failing to parse, raise that number first.
- **Adding an option to `buildSystemPrompt` has now dropped a sibling field TWICE** (`f101935` dropped `templates`, this session's working tree had dropped `char_budget` — caught before commit by tracing the field, restored). The call site is a wide object literal where an edit-in-place silently removes the old key and nothing type-errors, because every field is optional. Any change there: diff the call site key-by-key against `DrafterDecisionOpts`.

**2026-07-19 — Perf goal met (every page <500ms warm, incl. idle-return); middleware IS the auth boundary — never add a decode-only fast path.** Server pages read with the service-role key (no RLS) and pages-side getUser() is a local cookie decode, so the middleware remote check is the ONLY place the session token's signature is verified. Current design: verify-once-then-cache (5 min fresh, then pass + background re-verify up to 1 h; never-seen tokens always block). A decode-only expiry check was written 07-18 and caught 07-19 before commit — a hand-built cookie would have read everything. Test any middleware change with a forged-cookie curl (expect 307). WATCH: instrumentation.ts now HEAD-pings Supabase every 45s (keeps 3 connections warm, runs in prod too, ~1MB/day) — given the June egress history, glance at Supabase egress once this week to confirm it stays flat. Feed page + /api/feed/list now share one cached pipeline (`apps/web/app/_lib/feed_items.ts`); don't re-fork them. Full numbers: `.claude/perf-500ms-log.md`.

**2026-07-18 — Operator alerting LIVE; one Jake-only step left: add `HEALTHCHECKS_PING_URL_CLOUD=https://hc-ping.com/bbeada63-f1ac-40a0-bd6a-1a69d798217e` to the Render service env.** The cloud check is already armed, so until that var lands healthchecks WILL email a "down" alert within ~a day — that email means "finish the Render step," not an outage. Laptop check (6f79f088) fully live via `.env.local`; first real ping at the next noon launchd run. Reference: operator emails come from `packages/tools/src/notify.ts` (recipient = `policy.alerts.email` → fallback owner login email; set to jakeawatson91 on all 4 workspaces because Resend testing mode only delivers to the Resend account's own address until a domain is verified at resend.com/domains — then the knob can be cleared). Sweep-alert dedupe = `health_alert` fingerprint rows in events (re-emails only when the RED set changes). Verified in Inngest docs: NO native missed-run alert exists; failure handlers only fire when runs execute and run through Inngest itself — don't re-litigate the two heartbeats.

**2026-07-14 — Sudden runs fully in the cloud; watch the first unattended cycle.** Everything from the 07-13/14 session is pushed + live on Render (progress_log 2026-07-13 + 2026-07-14). WATCH, don't fix:
- First fully-automatic cycle: 04:00 UTC research tick (30-search budget) → enricher via the NEW direct dispatch → 14:30 UTC advance drafts. Check `research_completed` markers have `results_created > 0` and new facts carry non-score fact names.
- ATS re-activated 02:5x UTC 07-14 with a manual `source.run` in flight (full probe pass takes ~35 min; verify `last_run_at` moved and whether the 34 domained accounts produced hiring signals). Daily 13:00 UTC after that.
- 3 approvals pending on Sudden (CBC, SOOP, +1). Approving now sends to agentcrm91@gmail.com (`override_to` set 07-14; real client sending still needs a verified Resend domain eventually). Sudden approve→send has ZERO data points — have Jake approve one as the delivery test.
- Rescore cron will chew dogfood's ~1850 never-scored candidate entities as no-LLM noops over ~2 days, then go quiet ({candidates, rescored, noop} in Inngest output). If `noop` stays pinned at 50 past ~07-16, something's re-queueing.
- Sudden sweep `score_distribution` RED persists until research facts spread scores — expected.
- OPEN (quality, not blocking): enricher subscription similarity threshold dropped 25/28 research signals (research now bypasses it, but organic signals still face it — threshold 0.3 may be too high for this workspace); burst coalescing enriches only the FIRST research article per batch, the other N-1 distinct articles are skipped — a batch-aware enrich (one run over all results) would capture more per Exa dollar.
- OPEN: Hunter finds nobody for Sudden (0/6 pulls). Real contact coverage needs the Explorium/Apollo connector (existing backlog item).

**2026-07-10 — Sudden pipeline VERIFIED LIVE end-to-end (supersedes the 07-03/07-06 "not done" notes).** 1813/1961 accounts scored, 98 contacts scored (44 clear the 0.5 bar), advance pass created 2 real cited drafts (CBC/Radio-Canada, SOOP) now pending approval. Daily 14:30 UTC cloud cron runs it without Jake's laptop. Full detail in progress_log 2026-07-10. (This block's open items — scorable_types at creation, domain gap, ATS inactive, override_to null — all closed 07-13/14; see the block above.)

**2026-07-03 — Sudden (real client) workspace onboarding IN PROGRESS, handed to Jake mid-setup — do not assume it's done.** af602fa1 is currently PAUSED (`policy.pipeline.state='paused', scope='all'`; `policy.research.searches_per_run=0`) to protect shared DeepSeek/Hunter/Exa quota during the Sudden burst — restore via `scripts/_quiet_dogfood_for_sudden_burst.ts continue` once Sudden is stable; don't leave it paused indefinitely, it has no other reminder besides the amber pipeline banner (the research-budget-zero has none at all). Sudden's workspace was created and deleted twice this session (once corrupted by a domain-resolution bug, once during a DeepSeek balance outage, both now resolved) — Jake is setting the real one up himself through the actual UI per his explicit request, not yet re-imported as of session end. Real bugs fixed: (1) CSV import silently merged unrelated companies into one account when a "domain" column held a social-profile URL — guarded in `normalizeDomain()` (`packages/tools/src/ingest.ts`); (2) `ingest.ts:170`'s existing-account preload now chunks its `.in()` query; (3) the onboarding wizard never auto-created a drafter subscription, so every non-dogfood workspace could score/enrich but never draft — fixed in `api/workspaces/create/route.ts`. CSV import UI now exposes arbitrary column→fact mapping; the wizard now streams live progress instead of a dead button. **Open, not started:** Jake asked to consolidate contact-provider (Hunter/Explorium) config — currently scattered across Settings→Workspace (`max_contact_pulls_per_run`, daily), Settings→Connectors (`hunter_monthly_cap`, calendar-month, primary/fallback selector) — "there shouldn't be 5 places to set this up." **Open risk:** `hunter_monthly_cap` is unset/unlimited on both workspaces — this is the ONLY cap on the real-time drafter's own per-signal Hunter lookup (fires on every qualifying signal, not just the daily batch); the daily "contact lookups per run" cap does not bound it. Hunter has ~39 credits total remaining, shared across both workspaces.

**2026-07-02 evening — full-platform review PASSED; cloud advance tick CONFIRMED live (14:30 UTC cron fired; second 16:03 run was the on-demand verification, different accounts, no double-billing).** Sweep now all-green (source_concentration no longer counts internal research output). Commit `2e75030` (5 fixes: sweep, chat drafts scope + truncation totals, wizard/sources dev 500s, derive no-invented-claims) is LOCAL-ONLY — **Jake must `git push origin main`**. Two more Jake-only items: (1) launchd backstop is NOT loaded + plist hour wrong — two paste-ready commands in progress_log; (2) approve→send has one data point ever (May 30, pre-rewrite) — click Approve on one pending draft before client deploy. Test workspace `b4c5fc97` "ONBOARDING-TEST (safe to delete)" exists from wizard verification (wizard works, 270s latency, non-SaaS derive quality good).

**2026-07-02 — enrichment chain fixed end-to-end and live-proven (see progress_log entry).** Inngest is back (IN-XS, 500K events/mo) and is the primary runtime again. Daily ownership now: research dispatcher 0 */4 UTC → researchRunner → realtime enricher/rescore; NEW `advance-accounts-daily` cron 14:30 UTC does contacts+drafts in the cloud; launchd 16:00 UTC is a backstop that skips advance if the cloud ran <12h ago. Four fixes shipped (8a40246, 7cbd5e2): identity gate fail-closed + junk-page reject; pause scope 'contacts' vs 'all' (Hunter wall no longer blocks drafting); scoring no longer writes bogus ~0.35 on embedding failure ("cosine 0.00"); zero-fact runs heal missed rescores. Caps are in Settings→Workspace UI now (searches/run, contact pulls/run, drafts/run). WATCH: first cloud advance tick tomorrow ~14:30 UTC; launchd should log "skipped — cloud advance ran Nh ago" at 16:00.

## Direction

Agent-first CRM. Primary user is the agent; humans intervene only at exception approvals. Wedge is the abstraction layer above commodity DB (Postgres + pgvector + RLS), not the DB itself.

**Architecture is the moat.** Events + facts + provenance + replay + concurrency. Knowledge bases, drafter formula, NL config, constitution field, meta-agent routing — all commodities. Never pitch a surface feature as defensible.

**v0 strategy:** Build foundation + abstraction layer, prove it's measurably better for agent workloads than HubSpot via a benchmark suite, then pick a use case once the architecture is validated. **Dogfood test case: use agent-crm to sell agent-crm itself to founders running with ≤1 salesperson** (CLAUDE.md updated 2026-05-17, supersedes the prior "Jake's job hunt" framing).

## Stack confirmed

- Supabase (Postgres + pgvector) — hosted
- Inngest cloud as durable runtime — vault-backed pg_net trigger publishes signals/facts/approvals to webhook
- Next.js 15 viewer on Render (Free tier, kept warm by cron-job.org ping at /api/health)
- **LLM via Vercel AI SDK** (as of 2026-05-27). `deepseek/<model>` → DeepSeek direct (`DEEPSEEK_API_KEY`); any other `<vendor>/<model>` → Vercel AI Gateway (`AI_GATEWAY_API_KEY`). Default = DeepSeek (flash bulk / pro drafter). OpenRouter and direct-OpenAI chat paths removed. Do NOT reintroduce per-provider fetch code or `@anthropic-ai/sdk` calls — Anthropic is reachable through the gateway by config. See `[[project_llm_routing_ai_sdk_gateway]]`.
- Embeddings stay OpenAI `text-embedding-3-small` (DeepSeek has no embeddings endpoint; pgvector dimension-locked).
- **Dev workflow**: `pnpm --filter web dev` against prod Supabase via .env.local — push only for prod cron / customer-facing changes. Dev runs turbopack (`next dev --turbopack`); **build runs webpack** (`next build`, dropped `--turbopack` 2026-05-31 — turbopack couldn't build the inngest module graph; see build note below). Cold first-route compile ~7-10s, subsequent route compiles 200ms-3s, warm tab navigations 150-500ms. If you hit `ENOENT: ... _buildManifest.js.tmp` errors, kill all `next dev` processes and `rm -rf apps/web/.next apps/web/.turbo` — usually a race between two parallel dev servers. **Perf regressions recur** — do not break these rules: (1) `auth.ts:getUser()` uses `getSession()` (no remote call; middleware verified the token via its verify-once-then-cache — see the 2026-07-19 block up top; never weaken middleware to a decode-only check); (2) all read API routes are wrapped in `unstable_cache`; (3) SWR has `revalidateOnFocus: false`; (4) keep `instrumentation.ts` (undici keep-alive + 45s Supabase warm ping) — removing it re-adds ~300-400ms TLS handshakes on idle-return loads; note instrumentation changes need a dev-server restart, they don't hot-reload.
- **Turbopack `.js` → `.ts` extension-alias is broken in Next 15.5.18 for workspace packages** (despite `turbopack.resolveExtensions` config). Symptom: any route consuming `@agent-crm/primitives` or `@agent-crm/tools` 500s with `Module not found: Can't resolve './act.js'`. Fix shipped: every relative `./foo.js` import inside `packages/primitives/src/` and `packages/tools/src/` rewritten to `./foo.ts`. Turbopack accepts `.ts` directly; tsx accepts both. New files added to these packages MUST use `.ts` extensions in their imports.
- **Every new `@agent-crm/*` workspace package must be added to `transpilePackages` in `apps/web/next.config.mjs`.** Turbopack only applies `resolveExtensions` to listed packages. Caught when `@agent-crm/composio` was added but missed from the list: every page in the app 500'd with `Module not found: Can't resolve './scope.js'` even though the file was `.ts` — the package was treated as a non-transpiled library. Current list: primitives, tools, agents, db, inngest, composio.
- **Prod build is fragile — validate `next build` locally before any deploy (2026-05-31).** The web app bundles the inngest functions (`app/api/inngest/route.ts` → `inngest/functions/index.ts`), whose `.ts` sources use explicit `./foo.js` specifiers. Turbopack's `resolveExtensions` only resolves *extensionless* imports, not explicit `.js`→`.ts`, so `next build --turbopack` died with 62 Module-not-found errors. Fix in `apps/web/next.config.mjs`: build with webpack + `webpack.resolve.extensionAlias { '.js': ['.ts','.tsx','.js'] }`. Separately, `next build`'s tsc/eslint phase rejects the *packages'* explicit `./foo.ts` imports (TS5097) **and hung 88 min in-sandbox**, so `typescript.ignoreBuildErrors` + `eslint.ignoreDuringBuilds` are set (types still checked via each package's own `tsc --noEmit`). Net: the `.ts`-rewrite that fixed turbopack dev is what breaks `next build`'s typecheck.

## Hard rules (in CLAUDE.md + memory)

- **Agent-first or it doesn't ship.** Banned: pipeline views, sortable tables, kanban, batch ops, in-app feeds. Allowed human surfaces: approval queue, audit (verify agent state), config.
- **No new agents.** Closed set: claim_poster, drafter, enricher. Solve via tools, prompts, post-processors.
- **Real, scalable solutions.** No app-side fetch hacks for things that belong in the DB.
- **Not sending emails.** Drafts stay in Inbox; human still copy-pastes.
- **Banned words in any output to Jake:** substrate, gates, primitive, wedge, abstraction layer, predicate (as jargon), moat (vaguely). Use plain alternatives.

## v1 Benchmark results (current — supersedes v0 token claims)

**Headline (use this in pitches):**
- **Production agent-crm is 12% cheaper than Twenty.com, 2-7× cheaper than HubSpot, and 5-16× cheaper than Day.ai and Attio** on agent token cost across 3 workloads (draft / brief / score). Mean cost: $0.000475/action. 702 runs total, 5 platforms, all receipts saved. `benchmark/v1/WRITEUP.md`.
- Architectural gap is structural — bundled-read CRMs (agent-crm, Twenty) crush tool-loop CRMs (HubSpot, Day.ai, Attio) by 2-9× regardless of HubSpot's field selection. The tool-loop overhead can't be optimized away by the client.
- Production text format (the actual format used in `inngest/functions/agent_logic.ts: buildUserPrompt`) is 14% cheaper than the flat-JSON projection v1 originally measured, because dense text format has less structural overhead than nested JSON.

**v0 Workload 1b superseded:** ~~4.22× cheaper drafter tokens~~ — measured against stubbed HubSpot data on gpt-4o-mini. v1 measures real HubSpot APIs on production DeepSeek-reasoner and produces honest ratios across 3 workloads. v1 numbers are what to cite going forward.

**Categorical capability gaps (still valid from prior sessions):**
- **Workload 3 — Concurrency:** HubSpot loses 96% of 50 concurrent writes silently; agent-crm loses 0%
- **Workload 5 — Provenance:** agent-crm walks fact → event → actor → prompt chain in one join; HubSpot has 0 hops past the prose blob
- **Workload 6 — Replay:** agent-crm reconstructs full state at any past timestamp via one RPC; HubSpot has no equivalent

**Dead claim (do not cite):**
- ~~Workload 1a — Single-decision token cost 1.28×~~ — collapsed on re-validation. Numbers flipped on current data; single-tool workloads measure serialization format, not architecture. Marked DEPRECATED in `BENCHMARK.md`.
- ~~Workload 1b — 4.22× cheaper drafter tokens~~ — superseded by v1 (see above).

**Deferred (still open):**
- Hallucination rate per draft (n=1 anecdote suggests HubSpot fabricates more, but no LLM-judge harness to measure systematically)
- Pain-extraction yield on real production signals (only validated on synthetic fixtures)
- Quality eval at N=30+ blind-scored outputs per platform on v1 dataset (v1 punted this; sample inspection showed comparable quality, no rigorous test)

Full reports: `benchmark/v1/WRITEUP.md` (current), `BENCHMARK.md` at root (v0, retained for history), `benchmark/report/drafter_cost.md` (v0 drafter detail).

## What's Built

Full current system map moved to `architecture.md`.

## Known issues (deferred)

- **`unstable_cache` invalidation not wired to mutations.** Gates, feed, entities, and health are cached server-side (tags: 'gates', 'feed', 'entities', 'health'). Mutation routes (e.g. `gates/decide`) must call `revalidateTag('gates')` after writes or users will see stale data for up to the TTL (15-60s). Add these as the write paths are touched.
- **WATCH (2026-06-28): confirm Supabase egress drops in next 24-48h.** Root causes were the entities/index route (30s cache TTL × 2.5MB response = 2GB+/day), health actionDistribution (reading 10K post bodies × 2 windows at 60s TTL), and feed/list (20s TTL). All three fixed in commit `60171bd` and pushed. If egress is still high: check for other routes with short TTLs hitting Supabase on every browser poll.
- **launchd daily enrichment job (`sh.jakewatson.agentcrm.enrich`) was broken** — `node tsx.mjs` bypassed pnpm's module linker so `@supabase/supabase-js` couldn't resolve. Fixed to `pnpm tsx`, reloaded. Runs daily 09:00. When Inngest resets ~2026-07-01, entityResearchDispatcher (4h cron) resumes — analyzed and acceptable (id+channel_id only, 308KB total).
- IndieHackers RSS feed returns 0 raw items (feed URL or content-type changed). Lenny's and TechCrunch parse fine.
- RSS false-positive entity creation: tightened again in 2026-05-15 prompt push, still imperfect
- Render free-tier host sleeps on idle → recurring feed-death. **Real root cause found + fixed 2026-06-16:** `/api/health` was 307-redirecting to login (auth middleware `PUBLIC_PATHS` didn't whitelist it), so every cron-job.org keepalive ping logged as failed → keepalive went dead → host slept → Inngest unreachable → dispatcher stopped. Fix: added `/api/health` to `apps/web/middleware.ts` whitelist (deployed, returns 200). Keepalive must still stay enabled; bulletproof option = always-on paid Render tier (no sleep, keepalive becomes optional).
- A handful of accounts have `.example` placeholder domains and can't get Hunter contacts
- No sending pipeline — drafts stay in Inbox forever; human copy-pastes manually
- Auto-mode classifier: plain `git push origin main` and single-purpose commits go through fine (3 pushes on 2026-07-19); what it blocks is blanket staging (`git add -A`) and compound `commit && push` one-liners — split into separate simple commands instead of asking Jake to run them
- Workspace.constitution + about embedding gives one noise fact (`focuses_on: product development`) score of 0.383 — close to the top pain fact (0.392). Tuning issue, not architectural. Address via tighter `about` text or predicate-aware boost when more outcome data accumulates.
- **End-to-end verification of architecture-as-product against a fresh real-estate workspace is deferred** — code path proven via verify_loop on dog-food workspace, but no second-vertical sanity check yet. Open question: do the wizard-derived `example_facts` and drafter `pain_points` for a non-B2B vertical actually produce sensible drafts?
- **Reply ingest** for the post-send loop is not wired. `outreach_cooldown_until` + `silenceSweep` cron are live; an `inbound_email` Resend webhook → fact assertion would close the loop. Subscription infra (fact_filter on `inbound_email` predicate) is already in place.
- **Embedding doesn't read `policy.llm.openai_api_key` yet** — only chat does. Per-workspace embedding keys would need a thin wrapper around `embed()`; deferred until a customer asks.
- **Persistence + mobile responsiveness on the intake widget.** Conversation resets on refresh; panel is fixed-width 440px so <460px viewports break. Both deferred.
- **Native Anthropic SDK.** Anthropic models route via OpenRouter slash-prefix (`anthropic/claude-sonnet-4-6`); no direct API integration. Deferred until Anthropic billing clears.
- **Per-workspace secrets table.** API keys live on `workspaces.policy` as a stopgap; a real `workspace_secrets` table with envelope encryption is the long-term move.
- **Connector marketplace / sharing across workspaces.** Today connectors are per-workspace rows in `sources`. No way to share a spec with another customer.
- **Workspace `policy.env` placeholder secrets silently block real `.env.local` resolution.** Resolution order is `policy.env[NAME]` → legacy policy field → `process.env`, with non-empty placeholder values winning. Caught a `sk-test-…` `OPENAI_API_KEY` on the dog-food workspace that was blocking every chat / enricher / classifier call. No validation on save. Worth a one-time audit on every workspace before pointing real traffic at it; longer-term, a "looks-like-test-key" lint on settings save would help.
- **Relationship-edge graph LIVE on af602fa1** (`policy.enrichment.resolve_entities=true`, deployed 2026-05-31). Enricher now writes `object_entity` edges (open vocab — any fact with object_entity set is an edge) and creates domain-grounded `candidate` entities; verified on dev (customer_of→Stripe, integrates_with→Notion, investor→Sequoia). Fills SLOWLY: only ATS source is active (daily 13:00 UTC, ~5 hiring_post signals/day), no entity-discovery sources on, last accounts created 05-19. Watch with `scripts/density_check.ts` (snapshots + delta to `.claude/density_log.jsonl`; baseline = 123 works_at edges, 0 relationship edges).
- **Candidate promotion deferred.** Candidates are thin nodes (name + is_a + domain; `scoreEntity` skips `attributes.candidate=true`). Promotion (embed + enqueue enrichment at ≥N connections) NOT built — set the threshold from real candidate volume once it accrues. Re-embedding is also still write-once-at-create (only `yc.ts`, default perspective); a debounced "re-embed on enrichment" belongs in the same promotion path.
- **Score-dimension evidence_fact_ids only populate on freshly-computed `icp_fit_breakdown` facts going forward (2026-07-29).** `graphProximity()` now returns which neighbor/edge facts fed the graph_proximity mean, persisted on the breakdown JSON, and Today's "moved on network proximity" driver line cites them via CiteChain — but existing breakdown facts written before this deploy have no `evidence_fact_ids` key, so the trace link won't appear until an account's next rescore. Not urgent, just don't be confused if a mover card shows the delta with no trace link yet. The three LLM-judged dimensions (industry_match/stage_match/signal_strength) still have no per-fact evidence captured at all — deferred, would need the rubric call to report back which facts it leaned on.
- **UI verification for the 2026-07-29 citation-linking pass was typecheck + assertion suites only, not a live browser check** — the Chrome extension wasn't connected in that session. Worth a real look at the Today page mover cards + EntityDetail score card + relationship graph next time you're in the app, to confirm the CiteChain/WhyThis wiring renders as intended (should be low-risk: typecheck passed and the components themselves are unchanged, just newly wired to more call sites).
- **Signal-sourcing decision OPEN** (was mid-discussion at wrap). Exa/HN/web content discovery returned garbage (the junk entities "contact", "Not specified"=sociable.co, and content fragments are the residue). Recommendation forming: pipe a structured provider (Clay etc.) through the existing inbound webhook/CSV ingestion per the "data comes in" direction; do NOT revive content-search-as-entity-discovery (query craft isn't self-serve for future users). Hiring posts are stack/role-heavy, relationship-light — relationship edges (customer_of/backed_by) need news/funding signals.
- **~60 `.is('supersedes', null)` reader sites still return the STALE pre-supersede fact.** Only the graph/scoring/query path + the `query_facts_by_similarity` RPC (migration 0035) were fixed 2026-05-31. Direction: newer fact points to older (`new.supersedes = old.id`), so `.is('supersedes', null)` yields the original. Correct pattern = exclude ids pointed at by another fact's `supersedes` (`excludeSuperseded` in `packages/primitives/src/relations.ts`). `loadBestContactScore` fixed 2026-06-01; feed icp_fit read fixed 2026-07-19 (was ALSO hitting the 1000-row cap unscoped — now scoped to in-window entities in `app/_lib/feed_items.ts`); chat's entity-lookup tool (`api/agent/intake/tools.ts` `queryEntities` single-id branch) fixed 2026-07-29, caught while wiring that same fact list up to CiteChain. Still ~57 sites open — no systematic sweep done, only fixed on contact.
- **Second variant of the same class, found 2026-08-04: an activeness check that filters by predicate FIRST cannot see a cross-predicate supersede.** `_sweep_company_description.ts` collected supersedes pointers from its own predicate-scoped query, so 13 facts moved to a different predicate still read as active and the fix looked like a no-op. Correct shape is to look up `.in('supersedes', ids)` across all predicates. Any read that answers "is this fact still current" while scoped to one predicate is suspect — worth checking alongside the `.is('supersedes', null)` sites above, since it's the same mistake wearing a different hat.

### Two-tier scoring + contacts (open items, 2026-06-01) — see `HANDOFF_2026-06-01.md`
- **Reach-out thresholds + score weights are UNCALIBRATED.** Jake flagged routing as "random and flimsy" — fair. The draft/research/drop cutoffs (0.65 / 0.5 / 0.35) and the 6-part score blend (industry .30 / stage .20 / signal .10 / evidence .20 / recency .10 / graph .10) are hand-picked defaults, tunable per workspace (`policy.routing`), fit to NO outcome data. They encode intuition, not validated reply/meeting signal. Needs a calibration loop (feed real outcomes back, fit the numbers). Biggest "is it real" gap. Same for `signal_strength`, which gates draft at ≥0.7 but is just an LLM 0-1 judgement.
- **Drafter doesn't enforce the constitution's hard rules.** deepseek-v4-flash put an em dash in the EarthaPro draft despite "no em dashes EVER." Fix = post-processor after the LLM (strip em dashes, run banned-phrase + `forbidden_field_terms` lists) instead of trusting the model. **Still live 2026-08-04** — fresh Sudden drafts on deepseek-v4-pro still emit em dashes. Note the em-dash ban is Jake's own voice preference, so it belongs in `policy.drafter.message_rules` (config) rather than the shared craft block, but a post-processor is the only thing that will actually hold.
- **`value_themes` stale for the tiny-team ICP.** Gate themes `hiring/headcount/token_cost/ai_integration` include AI-startup leftovers (token_cost, ai_integration). Corrected-ICP trigger is "hiring first salesperson / scaling outbound." Refresh alongside the ICP.
- **No OBSERVED contact-signal source.** Contact scoring's LLM signal slot works (rated a real funding post ~0 buying-intent, a "drowning in outbound" post 1.0 — correct), but nothing feeds it real events. The EarthaPro demo signal was hand-synthesized from Exa firmographics + the founder's Vibe resume, NOT a detected event — Jake caught this. Need a per-contact signal source (Exa for funding/hiring/posts) or it stays manual. Exa is already keyed in prod, ~free under the 1k/mo tier.
- **`enrich_contacts` autonomous path needs an Explorium REST client + key.** Action fully wired (`contacts.requested` event → `contactsRunner` → 3-day cooldown, all verified) but the `explorium` branch no-ops: the Vibe MCP is chat-session-only, not callable from Inngest. Build a ~60-line Explorium REST client keyed by customer `EXPLORIUM_API_KEY` (self-serve, 100 free credits, ~$0.04/credit). Hunter branch works today (~10% small-co coverage).
- **Account with ZERO contacts (decision RESOLVED 2026-08-04, direction only).** Jake's call: do NOT drop the contact requirement — the templates select on audience and every message opens with a first name, so a nameless DM is a worse message, not a faster one. Instead, find a contact and then draft. First step shipped (promote people already named in notes); the unbuilt half is chaining enrich→draft in ONE pass, so a hot signal doesn't age past `trigger_fresh_days` waiting for the next tick.
- **Dogfood ICP corrected.** Workspace `icp` was stale sim-ai config; reset to PRIMARY = tiny B2B founder-led (1-20, any vertical, no sales hire), SECONDARY = AI-forward B2B SaaS (10-200). Don't blend. Contacts: 173 total / 83 accounts; 5 ICP test accounts (EarthaPro/Latch/TackPilot/Ottomatiq/TrueRev) scored 0.67-0.71.

### Automation revival + drafter quality (2026-06-05) — see progress_log
- **Crons were dead ~4.6 days; root cause = host, not code.** Fixed: merged llm-ai-sdk-registry → main, auto-deploy pushed it live. Diagnose-fast next time: `scripts/_diag_sources.ts`. `pnpm hiring:run` (WORKSPACE_ID=af602fa1…) = instant manual fresh data. Live host: https://agent-crm-fm1f.onrender.com. Keepalive (cron-job.org → /api/health) must stay active.
- **Drafter quality rules are UNCOMMITTED (dev only) — must commit + deploy for the AUTOMATED drafter to use them.** `prompt_builders.ts`: removed the hardcoded opener examples the model was parroting verbatim; added a "callout must be real evidence of a pain we solve, else refuse" rule, a general "DON'T BEND THE SIGNAL TO FIT THE PITCH — signals cut both ways" rule, and a no-fabrication rule. `agent_logic.ts`: sanitizer now strips `${}`/`{{}}`/`<>` template tokens, and `to_email` falls back to `outreach.override_to` when no contact is linked. UI: "icp"→"score" relabel (feed badge, entity cards, sort option, chat score cards) since the displayed `icp_fit` was always the composite `icp_total`. Verified across 5 real accounts: relevant, varied, refuses on no-signal, pulls benchmark numbers where they fit.
- **af602fa1 drafter config populated (resolves the 2026-06-02 empty-`value_props` dormancy).** `about` rewritten (agent-first, graph-based, each claim explained + backed by real v1 benchmark numbers, plain voice, no jargon); `value_props`/`pain_points`/`tone_keywords` set; `outreach.override_to` = agentcrm91@gmail.com. All live in DB. Editable at **Settings → Workspace → About** (saving re-derives the structured fields).
- **All drafts show `to_email: null`** — no contacts linked to these accounts, so they route to `override_to` at send. Real recipients depend on the contact-enrichment path (Explorium/Hunter).
- **Routing gate confirmed, not broken:** automated drafts require composite score ≥ 0.65 (`DRAFT_ICP_TOTAL`, default; `policy.routing` unset). The "emails at 0.57" Jake saw came from manual triggers (chat draft tool + test scripts) that bypass `selectAction`.

### Scheduler fully revived (2026-06-16) — see progress_log
- **The real cause of the recurring "feed dead since X" was a 307 on the keepalive endpoint, not the host being mysteriously flaky.** See the updated Known-issue bullet above. Fixed in `apps/web/middleware.ts` (`/api/health` whitelisted), deployed, verified 200.
- **Prod (Render) was missing `DEEPSEEK_API_KEY`** → `source-run` threw `Missing DEEPSEEK_API_KEY for deepseek-direct model deepseek-v4-flash` on the role classifier, so no source produced signals even when dispatched. Jake added it to Render env. **Prod env must carry `DEEPSEEK_API_KEY` + `OPENAI_API_KEY`** (the only two keys the AI-SDK router reads; `deepseek-v4-flash` is the only configured model). `OPENROUTER_API_KEY` can be dropped from Render — router no longer reads it. Verified: injected a `source.run` event → prod source-run completed `status=ok`.
- **Free connectors can no longer auto-deactivate on zero yield.** The dispatcher yield-monitor (`shouldDeactivate`) now requires `connector.meta.cost === 'metered'`. Added optional `cost: 'free'|'metered'` to `ConnectorMeta` (default free); marked `exa` + `exa_contacts` metered. Fixes the `ats` self-kill death spiral (a quiet week of no new job posts is normal for a free diff connector, was killing it permanently). Deployed (`956999c`).
- ~~**WATCH:** confirm `ats` auto-runs at the 13:00 UTC daily tick~~ — CONFIRMED 2026-06-18. `pnpm status` shows `ats_hiring_main` ran ~15h prior, created 32 signals; the research dispatcher fired on the exact `0 */4` schedule (16/20/00/04/08/12 UTC). Crons are firing reliably with no manual help. (ATS reports `status=error` but the errors are benign — false-positive board rejections + one non-fatal 429; it still produced signals. See new item below.)
- **DEFERRED (designed, not built): weekly $ budget cap on metered connectors.** Per-workspace, auto-pause + email + rolling 7-day auto-resume. Sketch: connectors report `cost_usd` in `ConnectorResult` → `metered_usage` ledger (or reuse Composio usage table) → `workspaces.policy.metered_weekly_budget_usd` (null=unlimited, no default) → dispatcher skips metered sources for over-budget workspaces. Email via `sendOwnerAlert` (`packages/tools/src/notify.ts`, built 2026-07-18); `notify_on_gate.ts` email channel still unwired. Jake wants ~$2/wk on his workspace so Exa can run automatically without spend fear.

### Research/enrichment loop fixed + live (2026-06-18) — see progress_log
- **WATCH: confirm researchRunner actually produces output after the deployed runner-fix tick (first 4h tick ≥ 16:00 UTC 2026-06-18).** The dispatcher fix is confirmed live (150 `research_triggered` dispatched on the `0 */4` schedule), but `research_completed` / `research_error` / `research_result` are still **0** — the `0/0/0` signature of the old broken runner. The runner fix (`d5bedfc`) merged after the 12:00 tick, so no tick has run it yet. Check with **`pnpm research:check`**; if still `0/0/0` a tick after deploy, the runner is erroring → check the Inngest dashboard `research-runner` run history.
- **New read-only diagnostic CLIs** (on main): `pnpm status` (full pipeline: sources / signals-by-type / pipeline output / enrichment markers / pending gates) and `pnpm status <signal_type> [N]` to dump real signals; `pnpm research:check` for the enrichment loop specifically. Use these instead of the UI for "is it running / show me real signals" — the UI is an audit shell, not a query tool.
- **`ats` connector flags `status=error` on benign non-fatal errors** (false-positive board rejections like "probe matched but board does not mention plaid.com — rejected"; non-fatal `429`). It still creates signals. Cosmetic but misleading in `pnpm status` — consider only setting `status=error` on fatal errors, or splitting fatal vs. non-fatal in `last_run_summary`.
- **Feed silently freezes when the pipeline goes quiet.** `DEFAULT_SWR` has `revalidateOnFocus:false` + no polling, so a feed tab left open never refreshes; and the 400-row window means a burst of one post kind can push everything else out of view. No staleness signal. Optional fixes: a `refreshInterval` on the feed SWR, and/or a "newest post N h old" badge in `FeedHealth`. (Root of the "stuck on May 30" report — was a dead May 31–Jun 15 gap + stale view, not a data bug.)

### Enrichment recovery loop fixed (2026-06-18) — see progress_log
- **Root cause of "enricher re-extracts the same facts forever" was `recoverUnmatchedSignals`, now fixed + deployed + verified.** Nothing wrote the `subscription.matched` events-table row that the recovery cron (and `healthCheck.unmatched_signals` + the source-signals UI badge) read, so every signal looked unmatched forever and the 15-min cron re-emitted `signal.created` for the newest 25 each tick (one AgentMail signal hit 89 enrichment runs). `matchSignal` now writes one durable marker per signal. Live-verified: markers writing, AgentMail enrichment flatlined to 0/12h, workspace dispatch 0/2h. If you ever see an entity re-enriched on a loop again, check this marker is still being written.

### 1000-row-cap read bugs — class fixed (2026-06-20) — see progress_log
- **Inngest was at 89% of the 50k/month run limit; root cause was the same recovery loop, still re-emitting.** The 2026-06-18 fix made `matchSignal` *write* the `subscription.matched` marker, but `recoverUnmatchedSignals` *reads* markers with an unbounded select that silently caps at PostgREST's 1000 rows. Past 1000 markers (workspace had 5,120) it couldn't see most of them, so ~198 of every 200 recent signals looked unmatched and got re-emitted every 15 min — measured 3,452 matchSignal runs/24h for 1,073 distinct signals (3.2× re-processing), tracking ~115k runs/month. Fixed (`f6bad9f`): scope the lookup to candidate signals via `.in('target_id', ids)` (the marker's `target_id` IS the signal_id; verified 0 mismatches). Also fixed `healthCheck` stale-gate count (same cap → phantom hourly approval requests) and 8 connector + bulk-CSV dedup reads (cap → duplicate signals → more runs) via a new paginated `fetchSeenSignalTags()` (`bf93e3e`). **WATCH: confirm the matchSignal run rate drops on the next deploy + cron tick** — `subscription.matched` markers/24h should fall from ~3,450 toward the true new-signal rate (~330/day).
- **DEFERRED — some UI list reads still cap at 1000.** `entities/index` fixed 2026-07-23 (`46fd1ee`, now pages via `fetchAll`). Still open: `feed/list` (`icp_fit`) and the intake-tool rankings read workspace-wide with no limit. Cosmetic until a workspace exceeds ~1000 entities (then the list/ranking silently truncates). Fix before onboarding any workspace that large. `research.ts:99` left alone (per-entity bounded, can't exceed 1000).
- **Systemic root worth a guard — hit again 2026-07-23, this time in a data-integrity check, not just a UI list.** Any `.select()` scoped only by `workspace_id` (or by an `.in(ids)` over >1000 ids) with no explicit pagination is suspect — the result is only safe if you genuinely want ≤1000 arbitrary rows, which a membership/count/dedup/activity check never does. The 2026-07-23 archive-sweep bug (see top of file) is this same class: an unpaginated `.in()` silently returned zero rows and the sweep archived up to 500 real accounts a day believing they had no activity. A lint rule banning the shape would stop this class recurring (offered, not built).

### ATS re-emission fix + job-posting over-extraction trimmed (2026-06-24) — see progress_log
- **The two `ats` sweep reds from 06-22 were a dedup bug, not a sourcing problem.** Seen-jobs cache trimmed to `slice(-200)` every run, so boards with >200 open roles (SpaceX, Brex, OpenAI, Airbnb, Stripe) forgot their overflow and re-emitted it as "new" — 3,829 ATS signals/24h were really 723 distinct jobs. Fixed: keep every currently-live job id, age out only ids no longer on the board. Also added: opt-in `dedup_key` idempotency on `create_signal` (any connector), optional `max_new_signals_per_entity` cap on the ATS source config (no default = no cap). Commit `a56f8aa`, pushed, live once Render redeploys.
- **Job postings also got trimmed from 28-33 facts to ~1-5.** Enricher's DEPTH instruction said "extract every atomic detail"; rescoped to "extract details about the subject company, not the artifact's internals" — a job posting now yields `hiring_role`/`hiring_salary_range`/`hiring_location`/`recent_event`, not the candidate-requirement checklist. Verified live on a fresh signal (Monzo) and confirmed it doesn't pile more facts onto already-over-extracted entities (Lance correctly returned 0 new facts, "already captured").

### Health surface honest + sweep cap fixed (2026-06-22) — see progress_log
- **Health monitoring no longer cries wolf, but two REAL reds remain — both the `ats` hiring source flooding.** `ats` produces ~3,829 signals/24h = 80% of all ingestion at 21% uniqueness (near-duplicate hiring posts). That's the open work: **tune the `ats` source config** (which roles/companies/boards to pull) to cut wasted ingestion + the LLM cost of enriching posts that yield nothing. It's config, not code — don't bake source-specific rules in. The sweep's `signal_diversity:ats` + `source_concentration` reds are this; they're correct.
- **`systemHealthMonitor` was DELETED.** It opened a `system_health` approval every hour (de-dup guard hit a non-existent column → phantoms piled up). Health now lives ONLY in the on-demand sweep + read-only feed strip — system health is not approval-worthy (decide-and-notify). External escalation BUILT 2026-07-18: the `health-sweep` cron (`inngest/functions/health_sweep.ts`, 11:15/23:15 UTC) emails the operator when a workspace's RED set changes. Don't look for the monitor.
- **`apps/web/app/api/admin/health/route.ts` attribution panel still caps at 1000 channels.** Its `drafted_touches` / `scored_accounts` / action_distribution use `.in('channel_id', chIds)` where `chIds` is the capped channels read (this workspace has >1000 channels). Undercounts (inflates tokens-per-touch). Fix = the FK-join pattern now used in `reads.ts` stale_drafts + `sweep.ts` claims: `.select('..., channels!inner(workspace_id)').eq('channels.workspace_id', ws)`. Not done — it's the cost panel, not an alarm.
- **`agent_run_metrics.payload.actor_id` is `undefined` on every row.** Sweep `enricher_silence` groups all enrichers under `(unknown)`; harmless now (pagination makes the count correct → green) but per-agent silence detection can't tell two enrichers apart. Populate `actor_id` in the agent-run path that writes this event if a second enricher ever ships.
- **`score_signal_coupling` redefined:** denominator is now the enricher's own fact-bearing dispatches (`agent_dispatch_result.facts_asserted>0`), not "any new signal" (which was dominated by intentionally-filtered hiring posts). Skips below 5 such entities/24h. Low new-fact yield is normal here (most enrichment re-confirms known facts), so the check often skips — that's honest, not broken.

## Plan File

Most recent: `review-this-plan-claude-plans-jaunty-gro-goofy-lynx.md` — onboard Sudden as a new tenant + protect shared quota during the burst. Executed through the code/script fixes (see 2026-07-03 note above); the CSV import itself is NOT re-run yet as of session end — Jake is doing that step himself.

Prior: `enumerated-foraging-spindle.md` — Twenty steals. Phase 1 (auth + members + API keys + Docker self-host) + Phase 3 (settings reorg, external MCP binary, Supabase self-host) SHIPPED. Phase 2 (entities.kind → is_a fact) still PARTIAL — dual-state stable; 0032 column drop + `find_similar_entities` RPC rewrite parked with the finish plan. No evidence this session touched it.

Older plan-file history lives in `progress_log.md`.

## Open Questions

- Reactions / outcomes UI design (outcome post exists per approval decision, but no aggregated "outcomes I care about" view)
- Multi-tenant embedding strategy (defer to design partner)
- Memory hierarchy (L1 prompt → L4 cold) — design vs let it emerge from cost pressure
- Where Sim/MCP plugs in as the action layer
- pgvector HNSW quality at >100k vectors per index (test before assuming)
- TAM at the sharp end (ops teams >1K autonomous touches/month, ~2K companies)
- Per-tenant Hunter quota tracking (single workspace today)
- Pain-extraction yield on real production signals (only validated on synthetic fixtures so far)
- Hallucination rate per draft on HubSpot vs agent-crm (one anecdote, needs an LLM-judge harness to measure)
