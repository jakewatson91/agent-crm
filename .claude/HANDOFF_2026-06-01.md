# Handoff — contacts + two-tier scoring (2026-06-01)

Pick-up notes for next session. Full detail in `progress_log.md` (2026-06-01 entry) and `project_state.md` ("Two-tier scoring + contacts" block). Workspace = af602fa1.

## What shipped (built + verified)
- **Contacts:** 173 in workspace (was 49), 83 accounts covered. Source decided: **Vibe/Explorium > Hunter** (Hunter ~10% on small/young co's). Names-first writer `linkContactByProspectId` — no email, dedups on `prospect_id` fact, email bought just-in-time at draft.
- **Two-tier scoring:** account score (unchanged) + new `contact_score` (deterministic seniority/persona + one LLM `signal_strength` slot that fires only on real content). Stored as `contact_score`, never `icp_fit` → 0 account pollution. `scoreContact` in `packages/tools/src/scoring.ts`.
- **`enrich_contacts` action wired:** `selectAction` gate (`account≥0.6 & best_contact<0.5`) → `contacts.requested` event → `contactsRunner` (`inngest/functions/contacts.ts`) → provider dispatch. 3-day cooldown. Hunter branch live; Explorium branch no-ops (needs a key + REST client).
- **Bug fixes:** scoring staleness-guard now re-scores on ICP/policy change (was fact-only → `rescore-on-icp-change` cron was a no-op). `loadBestContactScore` supersede-read fix.
- **ICP corrected:** workspace `icp` was stale sim-ai config. Reset to PRIMARY = tiny B2B founder-led (1-20, any vertical, no sales hire); SECONDARY = AI-forward SaaS (10-200), noted not blended.

## The open question Jake raised (do this first)
**Reach-out thresholds + score weights are uncalibrated** — "random and flimsy," and he's right. Cutoffs (draft 0.65 / signal 0.7 / research 0.5 / drop 0.35) and the 6-weight blend are hand-picked, fit to NO outcome data. The framework is sound and per-workspace tunable (`policy.routing`); the numbers are guesses. Real fix = a calibration loop (log reply/meeting outcomes → fit the numbers). Until then, be honest that they're intuition, not signal.

## Next-step menu (pick with Jake)
1. **Drafter post-processor** — strip em dashes + run banned-phrase/`forbidden_field_terms` lists after the LLM. deepseek-v4-flash violated "no em dashes EVER" in the EarthaPro draft. Small, high-value.
2. **Real (observed) contact-signal source** — per-contact Exa pull for funding/hiring/posts, so signals are detected events, not hand-synthesized. Exa is already keyed in prod, ~free under 1k/mo. The EarthaPro demo signal was assembled by hand (Jake caught it).
3. **Refresh `value_themes`** for the tiny-team ICP — `token_cost`/`ai_integration` are AI-startup leftovers; real trigger is "hiring first salesperson / scaling outbound."
4. **Explorium REST client** (`~60 lines`) keyed by customer `EXPLORIUM_API_KEY` → makes `enrich_contacts` autonomous. Self-serve signup, 100 free credits, ~$0.04/credit. Vibe MCP can't be called from Inngest.
5. **Threshold calibration loop** — the real answer to the flimsy-thresholds question. Needs outcome data first.

## Open decision
- Account with ZERO `works_at` contacts → currently routes account-only (`best_contact_score` undefined). Should no-contact force `enrich_contacts`? Conservative default avoids false-enrich on legacy email-domain-linked contacts.

## Test data to reuse
- 5 ICP accounts scored 0.67-0.71: EarthaPro, Latch, TackPilot, Ottomatiq, TrueRev.
- EarthaPro full loop done: founder Courtney Krstich (contact 0.81, email `courtneykrstich@earthapro.com`), a draft was produced. Latch founder not found in Explorium/Exa.
- Scratch scripts in `scripts/_*.ts` (e.g. `_full_loop.ts`, `_draft_earthapro.ts`, `_score_contacts.ts`, `_contact_coverage.ts`).

## Key files touched
`packages/tools/src/{scoring.ts, contacts.ts, action_selector.ts, policy.ts, index.ts}` · `inngest/functions/{agent_logic.ts, contacts.ts, index.ts}` · `inngest/client.ts` · `apps/web/app/api/{inngest/route.ts, agent/intake/tools.ts, admin/routing-preview/route.ts}`
